#include "Checker.h"

using namespace std;

int main(int argc, char **argv) {
    Checker check;
    check.getFileName();
    check.goToStack();
    //if (isMatched(getTags())) // get tags and test them
    //cout << "The input file is a matched HTML document." << endl;
    //else
    //cout << "The input file is not a matched HTML document." << endl;

    //myStack->push('M');
    //myStack->push('a');
    //myStack->push('e');
    //myStack->push('t');
    //st.push('M');
    //st.push('a');
    //st.push('e');
    //st.push('t');

    //cout << st.peek() << endl;
    //cout << "Popping: " << st.pop() << endl;
    //st.push('x');
    //cout << "Peeking again: " << st.peek() << endl;

    //while(!st.isEmpty()){
    //  cout << "Popping: " <<st.pop() << endl;
    //}
  //cout << "is the stack empty? " << st.isEmpty() << endl;

  return 0;
}
  //delete myStack;
